# my-fin — 投資決策輔助系統

---

## 安裝

```bash
# 安裝 uv (Python 套件管理器)
curl -LsSf https://astral.sh/uv/install.sh | sh

# 安裝依賴
uv sync

# 安裝 Playwright (財報狗爬蟲需要)
uv run playwright install chromium
```

---

## 設定

### 1. 環境變數

```bash
cp .env.example .env
```

詳細的環境變數說明請參考 [`.env.example`](.env.example)。

### 2. 驗證安裝

```bash
make test
```

---

## 使用方法

### 🗓️ 使用流程

```
┌──────────────────────────────────────────────────────────────────────┐
│  每日凌晨  →  make daily            (日報：天候 + 體制 + 今日事件)   │
│  每週日    →  make weekly           (週報：績效反思 + 技能 vs 運氣)  │
│  本地執行  →  make scan             (完整掃描：動能 + 財報狗)        │
│            →  make retain           (單一標的完整驗證)               │
│  分離執行  →  make scan-momentum    (Phase 1: 動能評估)              │
│            →  make scan-fundamental (Phase 2: 財報狗爬蟲)            │
│            →  make summary          (Phase 3: 匯出 CSV)              │
│  任何時候  →  make monitor          (即時市場監控：VIX/DEFCON/HMM)   │
│  需要時    →  make sync-data        (同步經濟日曆 + 股票池)          │
└──────────────────────────────────────────────────────────────────────┘
```

> ⚠️ **`make scan*` / `make retain*` 只能本地執行**：需要 Shioaji 台灣 IP 連線 + Playwright 瀏覽器

---

### 核心指令

| 指令                                     | 說明                                                    |
| ---------------------------------------- | ------------------------------------------------------- |
| `make scan`                              | 完整掃描 (動能 + 財報狗)，結果推送至 JSON (⚠️ 本地專用) |
| `make scan-momentum`                     | 只執行動能評估 (⚠️ 本地專用)                            |
| `make scan-fundamental`                  | 只執行財報狗爬蟲 (⚠️ 本地專用)                          |
| `make retain SYMBOL=2330`                | 單一標的完整驗證 (⚠️ 本地專用)                          |
| `make retain-momentum SYMBOL=2330`       | 單一標的動能評估 (⚠️ 本地專用)                          |
| `make retain-fundamental SYMBOL=2330`    | 單一標的財報狗 (⚠️ 本地專用)                            |
| `make summary DATE=2026-01-02`           | 匯出指定日期的 JSON → CSV                               |
| `make stock DATE=2026-01-02 SYMBOL=2330` | 取得指定日期的單一股票資料                              |
| `make daily`                             | 日報：天候燈號 + 體制識別 + 掃描結果 Top 10 + 今日事件  |
| `make weekly`                            | 週報：週度覆盤，技能判定 (DSR/PSR)                      |
| `make monitor`                           | 即時市場監控：VIX/DEFCON/HMM 警報                       |
| `make sync-data`                         | 同步經濟日曆與股票池                                    |

> 💡 **分離執行優點**：進度條更準確、可斷點續跑、錯誤隔離

---

### GitHub Actions 自動排程

| 任務 | 觸發時間 (UTC+8) | 說明               |
| ---- | ---------------- | ------------------ |
| 日報 | 週一~五 00:00    | 根據前一交易日收盤 |
| 週報 | 每週一 00:00     | 回顧上週績效       |

> 📝 **注意**：`make scan` 已從 GitHub Actions 移除 (需本地執行)

---

### 開發工具

| 指令          | 工具        | 說明              |
| ------------- | ----------- | ----------------- |
| `make lint`   | Ruff        | 程式碼風格檢查    |
| `make format` | Ruff        | 程式碼格式化      |
| `make check`  | Ruff + Mypy | 全部檢查（CI 用） |
| `make test`   | Pytest      | 執行單元測試      |
